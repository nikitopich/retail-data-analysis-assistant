[pytest]
# Default run = the deterministic unit suite (no LLM, no BigQuery, no network).
# The DeepEval LLM-judge cases under evals/ are live and opt-in: run them
# explicitly with `pytest evals/` or `python -m evals.run`.
testpaths = tests
python_files = test_*.py
