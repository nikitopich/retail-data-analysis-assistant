# provided by the client
langgraph>=0.2.0
langchain-google-genai>=1.0.0
google-cloud-bigquery>=3.13.0
pandas>=2.0.0
python-dotenv>=1.0.0
langchain_core>=0.3.0
db-dtypes==1.2.0

# added for the prototype
langchain>=0.3.0
langgraph-checkpoint-sqlite>=2.0.0     # SqliteSaver: interrupt()/resume() persistence
arize-phoenix>=4.0.0                    # local tracing + UI
arize-phoenix-otel>=0.6.0              # phoenix.otel.register
openinference-instrumentation-langchain>=0.1.0

# automated tests for the test plan (evals/)
deepeval>=4.0.0                         # test runner framework + LLM judges (Gemini)
pytest>=8.0.0                           # runs evals/test_assistant.py
